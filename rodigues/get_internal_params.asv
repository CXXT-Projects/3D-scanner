% cc = load('Calib_Results.mat', ff);

function [result]  = get_internal_params()

mat = load('Calib_Results.mat');

length = 7;
result = zeros(3, 3, 7);

omc = mat.('cc');


for i = 1:length
    omc = mat.(strcat('omc_', num2str(i)));
    t = mat.(strcat('Tc_', num2str(i)));
    rotVecToMat = rotationVectorToMatrix(omc);
    out = [rotVecToMat, t];
    result(:, :, i) = out;
end
end
