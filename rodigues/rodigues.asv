% cc = load('Calib_Results.mat', ff);

function [result]  = rodigues()

mat = load('237.mat');

% length = 7;
% result = [];
% input = {};
% for i = 1:length
%     omc = load('Calib_Results.mat', strcat('omc_', num2str(i)));
%     
%     data = double(omc);
%     result(i) = rotationVectorToMatrix(data);
% end
